pre-push:
  jobs:
    - name: packages audit
      run: pnpm audit --audit-level=moderate

pre-commit:
  parallel: true
  jobs:
    - name: check lint
      run: pnpm lint:check {staged_files}

    - name: check format
      run: pnpm format:check

    - name: check type
      run: pnpm typecheck
